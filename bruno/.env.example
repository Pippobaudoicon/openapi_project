# Bruno secret variables template — safe to commit
# Copy this to .env and fill in real values. Never commit .env.

# Production tokens
companyToken=
visureToken=
oauthPassword=

# Test environment tokens (uncomment and swap when using Test environment)
# companyToken=
# visureToken=
# oauthPassword=
