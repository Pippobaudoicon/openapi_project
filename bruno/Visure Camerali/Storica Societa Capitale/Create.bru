meta {
  name: STORICA SOCIETA CAPITALE
  type: http
  seq: 1
}

post {
  url: {{visureBaseUrl}}/storica-societa-capitale
  body: json
  auth: bearer
}

auth:bearer {
  token: {{visureToken}}
}

body:json {
  {
    "cf_piva_id": "{{PIVA}}"
  }
}
