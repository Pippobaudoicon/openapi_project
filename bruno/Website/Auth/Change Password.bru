meta {
  name: Change Password
  type: http
  seq: 2
}

post {
  url: {{websiteBaseUrl}}/auth/change-password
  body: json
  auth: none
}

body:json {
  {
    "oldPassword": "test",
    "newPassword": "test"
  }
}
