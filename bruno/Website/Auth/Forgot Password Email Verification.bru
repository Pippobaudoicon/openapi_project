meta {
  name: Forgot Password Email Verification
  type: http
  seq: 4
}

post {
  url: {{websiteBaseUrl}}/auth/reset-password/{{TOKEN}}
  body: json
  auth: none
}

body:json {
  {
    "password": "test"
  }
}
